/** Écran Sites d'un client + accès synthèses et cartographie technique. */
import React, { useState, useCallback, useEffect, useMemo } from 'react';
import { View, Text, FlatList, TouchableOpacity, Modal, TextInput, Alert, Linking } from 'react-native';
import { COLORS, styles } from './styles.js';
import { listerSitesClient, creerSite } from './db.js';
import { getResumeSuppressionSite, supprimerSiteComplet } from './entityManagementDb.js';
import { sitesAvecGps } from './siteGeoDb.js';
import { SiteAddressManager } from './SiteAddressManager.js';
import { exporterDernieresVisitesClient } from './clientBatchExport.js';
function ClientSitesScreen({ route, navigation }) {
  const { clientId, nomClient } = route.params; const [sites,setSites]=useState([]),[modalVisible,setModalVisible]=useState(false),[gestionVisible,setGestionVisible]=useState(false),[nouveauNom,setNouveauNom]=useState(''),[nouvelleAdresse,setNouvelleAdresse]=useState(''),[exportClientEnCours,setExportClientEnCours]=useState(false);
  const charger=useCallback(async()=>{const liste=await listerSitesClient(clientId);setSites(liste);return liste;},[clientId]); useEffect(useCallback(()=>{charger();},[charger]));
  const sitesGps=useMemo(()=>sitesAvecGps(sites),[sites]),sansGps=sites.length-sitesGps.length,sansAdresse=sites.filter((s)=>!String(s.adresse||'').trim()).length;
  const ajouterSiteFn=async()=>{if(!nouveauNom.trim()){Alert.alert('Nom requis','Merci de saisir le nom du site.');return;}await creerSite({clientId,nomSite:nouveauNom.trim(),adresse:nouvelleAdresse.trim()||null});setNouveauNom('');setNouvelleAdresse('');setModalVisible(false);await charger();};
  const exporterDernieresVisites=async()=>{if(exportClientEnCours)return;setExportClientEnCours(true);try{const resultat=await exporterDernieresVisitesClient(clientId);if(resultat.annule)return;const ok=resultat.enregistres?.length||0,erreurs=resultat.erreurs?.length||0;Alert.alert('Export client terminé',`${ok} dernière${ok>1?'s':''} visite${ok>1?'s':''} exportée${ok>1?'s':''}${erreurs?` · ${erreurs} erreur${erreurs>1?'s':''}`:''}.`);}catch(e){Alert.alert('Export impossible',String(e?.message||e));}finally{setExportClientEnCours(false);}};
  const ouvrirSite=(site)=>navigation.navigate('SiteVisites',{siteId:site.id,nomSite:site.nom_site});
  const confirmerSuppressionSite=async(site)=>{try{const resume=await getResumeSuppressionSite(site.id);if(!resume)return;Alert.alert('Supprimer ce site ?',`« ${resume.nom_site} » contient ${resume.visites} visite(s) et ${resume.equipements} équipement(s) permanent(s). Tout sera définitivement supprimé.`,[{text:'Annuler',style:'cancel'},{text:'Supprimer tout',style:'destructive',onPress:async()=>{try{await supprimerSiteComplet(site.id);await charger();}catch(e){Alert.alert('Suppression impossible',String(e.message||e));}}}]);}catch(e){Alert.alert('Suppression impossible',String(e.message||e));}};
  const ouvrirGoogleMaps=async(site)=>{const lat=Number(site.latitude),lng=Number(site.longitude),query=Number.isFinite(lat)&&Number.isFinite(lng)?`${lat},${lng}`:String(site.adresse||'').trim();if(!query)return;try{await Linking.openURL(`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(query)}`);}catch{Alert.alert('Google Maps',"Impossible d'ouvrir Google Maps.");}};
  const ResumeLocalisation=()=> <View style={{marginBottom:18}}><Text style={styles.sectionLabel}>Localisation du parc</Text><View style={{marginTop:8,padding:14,borderRadius:14,backgroundColor:'#fff',borderWidth:1,borderColor:'#E3E5E8'}}><View style={{flexDirection:'row',gap:10}}><View style={{flex:1}}><Text style={{fontSize:22,fontWeight:'800'}}>{sitesGps.length}</Text><Text style={{color:COLORS.muted,fontSize:12}}>sites avec GPS</Text></View><View style={{flex:1}}><Text style={{fontSize:22,fontWeight:'800'}}>{sansGps}</Text><Text style={{color:COLORS.muted,fontSize:12}}>à positionner</Text></View></View></View></View>;
  return <View style={{flex:1,backgroundColor:COLORS.bg}}><FlatList contentContainerStyle={styles.content} data={sites} keyExtractor={(item)=>item.id} ListHeaderComponent={<View><ResumeLocalisation/><View style={{flexDirection:'row',gap:8,marginBottom:8}}><TouchableOpacity style={[styles.btnSecondary,{flex:1}]} onPress={()=>navigation.navigate('ClientPatrimoine',{clientId,nomClient})} disabled={!sites.length}><Text style={styles.btnSecondaryText}>📊 Synthèse</Text></TouchableOpacity><TouchableOpacity style={[styles.btnPrimary,{flex:1}]} onPress={()=>navigation.navigate('ClientTechnicalMatrix',{clientId,nomClient})} disabled={!sites.length}><Text style={styles.btnPrimaryText}>▦ Cartographie technique</Text></TouchableOpacity></View><View style={{flexDirection:'row',gap:8,marginBottom:8}}><TouchableOpacity style={[styles.btnSecondary,{flex:1}]} onPress={()=>setGestionVisible(true)}><Text style={styles.btnSecondaryText}>⚙️ Gérer les sites</Text></TouchableOpacity><TouchableOpacity style={[styles.btnPrimary,{flex:1}]} onPress={()=>setModalVisible(true)}><Text style={styles.btnPrimaryText}>+ Ajouter un site</Text></TouchableOpacity></View><TouchableOpacity style={[styles.btnPrimary,{marginBottom:8}]} onPress={()=>navigation.navigate('Report',{clientId})} disabled={!sites.length}><Text style={styles.btnPrimaryText}>📄 Générer un rapport PDF / Word</Text></TouchableOpacity><TouchableOpacity style={[styles.btnSecondary,{marginBottom:14}]} onPress={exporterDernieresVisites} disabled={exportClientEnCours||!sites.length}><Text style={styles.btnSecondaryText}>{exportClientEnCours?'Export en cours…':'⇩ Exporter les dernières visites du client'}</Text></TouchableOpacity>{(sansAdresse>0||sansGps>0)?<View style={{backgroundColor:'#FFF8E7',borderWidth:1,borderColor:'#F0D99B',borderRadius:12,padding:10,marginBottom:14}}><Text style={{color:'#7A5700',fontSize:12,fontWeight:'700'}}>{sansAdresse?`${sansAdresse} site(s) sans adresse`:''}{sansAdresse&&sansGps?' · ':''}{sansGps?`${sansGps} sans point GPS`:''}</Text></View>:null}<View style={styles.sectionHeaderRow}><Text style={styles.sectionLabel}>Sites</Text><Text style={{color:COLORS.muted,fontSize:12}}>{sites.length}</Text></View></View>} renderItem={({item})=>{const hasGps=Number.isFinite(Number(item.latitude))&&Number.isFinite(Number(item.longitude));return <TouchableOpacity style={styles.card} activeOpacity={0.7} onPress={()=>ouvrirSite(item)}><View style={{flex:1}}><Text style={styles.cardTitle}>{item.nom_site}</Text>{item.adresse?<Text style={styles.cardSub}>{item.adresse}</Text>:<Text style={{color:'#A26A00',fontSize:12}}>Adresse à renseigner</Text>}{hasGps?<TouchableOpacity onPress={(e)=>{e?.stopPropagation?.();ouvrirGoogleMaps(item);}}><Text style={{color:COLORS.primary,fontSize:11,fontWeight:'800',marginTop:5}}>📍 Google Maps ↗</Text></TouchableOpacity>:null}</View><View style={[styles.badge,item.statut==='Actif'?styles.badgeActif:styles.badgeInactif]}><Text style={[styles.badgeText,item.statut==='Actif'?styles.badgeTextActif:styles.badgeTextInactif]}>{item.statut||'Actif'}</Text></View><TouchableOpacity onPress={(e)=>{e?.stopPropagation?.();confirmerSuppressionSite(item);}} style={{minWidth:42,minHeight:42,alignItems:'center',justifyContent:'center',marginLeft:6}}><Text style={{color:COLORS.red||'#B42318',fontSize:18,fontWeight:'800'}}>✕</Text></TouchableOpacity></TouchableOpacity>;}} ListEmptyComponent={<View style={styles.empty}><Text style={styles.emptyText}>Aucun site pour ce client.</Text></View>}/><Modal visible={modalVisible} transparent animationType="fade"><View style={styles.modalOverlay}><View style={styles.modalSheet}><Text style={styles.modalTitle}>Nouveau site</Text><TextInput style={styles.input} placeholder="Nom du site" value={nouveauNom} onChangeText={setNouveauNom}/><TextInput style={[styles.input,{marginTop:10}]} placeholder="Adresse (optionnel)" value={nouvelleAdresse} onChangeText={setNouvelleAdresse}/><View style={styles.modalActions}><TouchableOpacity style={styles.btnSecondary} onPress={()=>setModalVisible(false)}><Text style={styles.btnSecondaryText}>Annuler</Text></TouchableOpacity><TouchableOpacity style={styles.btnPrimary} onPress={ajouterSiteFn}><Text style={styles.btnPrimaryText}>Créer</Text></TouchableOpacity></View></View></View></Modal><SiteAddressManager visible={gestionVisible} clientId={clientId} sites={sites} onClose={()=>setGestionVisible(false)} onChanged={charger}/></View>;
}
export { ClientSitesScreen };
